{% extends 'base.html.twig' %}

{% block title %}{{ invoice.invoiceNumber }}{% endblock %}

{% block body %}
    <div class="d-flex justify-content-between align-items-start mb-4">
        <div>
            <a class="link-secondary text-decoration-none" href="{{ path('crm_property_show', {id: invoice.property.id}) }}">Back to Property</a>
            <h1 class="h2 mt-2 mb-1">{{ invoice.invoiceNumber }}</h1>
            <p class="text-body-secondary mb-0">{{ invoice.status|replace({'_':' '})|title }}</p>
        </div>
    </div>

    <div class="row g-4">
        <div class="col-lg-8">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5">Line Items</h2>
                    <table class="table align-middle">
                        <thead>
                            <tr>
                                <th>Description</th>
                                <th>Qty</th>
                                <th>Unit Price</th>
                                <th>Total</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for lineItem in lineItems %}
                                <tr>
                                    <td>{{ lineItem.description }}</td>
                                    <td>{{ lineItem.quantity }}</td>
                                    <td>${{ (lineItem.unitPriceCents / 100)|number_format(2) }}</td>
                                    <td>${{ (lineItem.totalCents / 100)|number_format(2) }}</td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="4" class="text-center text-body-secondary py-4">No line items.</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                        <tfoot>
                            <tr>
                                <th colspan="3" class="text-end">Total</th>
                                <th>${{ (invoice.totalCents / 100)|number_format(2) }}</th>
                            </tr>
                        </tfoot>
                    </table>
                </div>
            </div>
        </div>
        <div class="col-lg-4">
            <div class="card shadow-sm">
                <div class="card-body">
                    <h2 class="h5">Update Status</h2>
                    <form method="post" action="{{ path('crm_invoice_update_status', {id: invoice.id}) }}" class="d-grid gap-3">
                        <input type="hidden" name="_token" value="{{ csrf_token('invoice_status_' ~ invoice.id) }}">
                        <select class="form-select" name="status">
                            {% for status in ['draft', 'sent', 'partially_paid', 'paid', 'overdue', 'void'] %}
                                <option value="{{ status }}" {% if invoice.status == status %}selected{% endif %}>{{ status|replace({'_':' '})|title }}</option>
                            {% endfor %}
                        </select>
                        <button class="btn btn-outline-primary" type="submit">Save Status</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
